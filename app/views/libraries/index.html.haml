%h1 Libraries
%table
    %thead
        %tr
            %th Name
            %th Address
            %th Phone Number
            %th # Books
            %th{:colspan => "3"}
            %th= link_to 'Back', root_path
    %tbody
        - @libraries.each do |library|
            %tr
                %td= library.name
                %td= library.address
                %td= library.number
                %td= library.books.count
                %td= link_to "Show", library_path(library)
                %td= link_to 'Edit', edit_library_path(library)
                %td= link_to 'Delete', library, method: :delete, data: { confirm: 'Are you sure?' }

%br/
= link_to 'New Library', new_library_path